############################
###        USER          ###
############################
### Aufgabe
Extrahiere aus dem folgenden OCR‑Rohtext **exakt** die zehn definierten Felder und gib sie als JSON‑Objekt zurück.

### Wichtig zum Input
- Jeder Eintrag ist eine Zeile mit `[y=...]`, z.B.
  `[y=585] "Rechnung 20240139"`
- **Sortiere** und **gruppiere** nach y‑Werten, um Kopf‑/Detail‑/Fußbereich logisch zu trennen.

### Kurzregeln
1. Fehlende Werte → `null`.
2. total_amount & tax_rate: Zahl mit **Punkt**, zwei Nachkommastellen, keine Tausendertrennung.
3. Nur die zehn Schlüssel in vorgegebener Reihenfolge.
4. IBAN: erste **[A‑Z]{2}[0‑9A‑Z]{13,32}** nach »IBAN«/**»Konto«».
5. USt‑Id nach EU‑Regex; sonst `null`.
6. Ergebnis **ausschließlich** in einer Zeile zwischen `<json_output>`‑Tags.

### OCR‑Text mit y‑Koordinaten
```plaintext
